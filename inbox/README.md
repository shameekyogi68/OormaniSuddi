# Inbox

`today.md` / `today.json` / `today.txt` are **tip sheets**, not copy.

`scripts/fetch_daily_news.py` writes them. Open every `source_url`. Confirm
every fact. Then write `editions/YYYY-MM-DD.json`.

A tip without a URL, and without own reporting, never becomes an edition.
