# Decision → enforcement

_Generated by `docs/_build_traceability.py`. Do not edit._

A decision with no test behind it is a paragraph, not a rule. This is
the list of which is which. Some entries are honestly untestable —
those carry a reason rather than a test.
## Coverage

- **52/77** decisions are named by at least one test
- **23** are regression-guarded by the golden fingerprints — a change moves pixels and the hash fails, which catches a regression without asserting the rule
- **2** are recorded as not mechanically testable, each with a reason
- **0 are enforced by nothing**


## D1 · Leading comes from the em, not the font's metrics

- **Regression-guarded by the golden fingerprints** (leading from the em). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D2 · Text is drawn on baselines, never at box positions

- **Regression-guarded by the golden fingerprints** (baseline drawing). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D3 · Tracking is ignored on Kannada

- **Regression-guarded by the golden fingerprints** (no tracking on Kannada). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D4 · Latin faces fall back to Kannada automatically

- **Regression-guarded by the golden fingerprints** (Latin falls back to Kannada). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D5 · A photograph fades its own alpha; it is never painted over

- **Regression-guarded by the golden fingerprints** (photographs fade their own alpha). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D6 · The scrim ramp is a smoothstep, not a power curve

- **Regression-guarded by the golden fingerprints** (smoothstep scrim ramp). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D7 · Crop focal point defaults to (0.5, 0.42), not centre

- **Regression-guarded by the golden fingerprints** (focal default (0.5, 0.42)). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D8 · Grain is mandatory

- **Regression-guarded by the golden fingerprints** (grain is mandatory). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D9 · Category colour lives only on the rail

- **Regression-guarded by the golden fingerprints** (category colour only on the rail). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D10 · Hairlines, square corners, no canvas border

- **Regression-guarded by the golden fingerprints** (hairlines, square corners, no border). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D11 · Layouts flow content; they never position it

- **Regression-guarded by the golden fingerprints** (layouts flow content). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D12 · Item gaps must clearly exceed line gaps

- **Regression-guarded by the golden fingerprints** (item gaps exceed line gaps). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D13 · Reels are full-bleed, not a photo band plus a text band

- **Regression-guarded by the golden fingerprints** (reels are full-bleed). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D14 · A reel scene carries a headline and one supporting line

- **Regression-guarded by the golden fingerprints** (headline plus one supporting line). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D15 · Scene duration follows reading time

- **Tested by:** `tests/test_contract.py`

## D16 · Nothing cuts on

- **Regression-guarded by the golden fingerprints** (nothing cuts on a hard frame). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D17 · Audio is normalised to −14 LUFS / −1.5 dBTP

- **Tested by:** `tests/test_contract.py`

## D18 · Safe zones are structural, not advisory

- **Tested by:** `tests/test_contract.py`

## D19 · Genuineness is enforced in the type system, not the style guide

- **Tested by:** `tests/test_contract.py`

## D20 · Latin numerals, and never both

- **Tested by:** `tests/test_contract.py`

## D21 · Everything renders at 2× and downsamples once

- **Tested by:** `tests/test_golden.py`

## D22 · The clock is injectable

- **Tested by:** `tests/test_contract.py`, `tests/test_golden.py`
- Enforced in: `brand/reach.py`

## D23 · Golden tests render a frozen fixture, never a live edition

- **Tested by:** `tests/test_contract.py`, `tests/test_golden.py`

## D24 · Translucent text goes through its own layer

- **Regression-guarded by the golden fingerprints** (translucent text on its own layer). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D25 · Every post and every slide carries a visual

- **Tested by:** `tests/test_contract.py`

## D26 · Reel timing is honest about Kannada reading speed

- **Tested by:** `tests/test_contract.py`

## D27 · Standing copy lives in tokens, not in templates

- **Tested by:** `tests/test_contract.py`

## D28 · Never seed anything from `hash()`

- **Tested by:** `tests/test_golden.py`

## D29 · Legal guards are validation rules, not guidance

- **Tested by:** `tests/test_calendar.py`, `tests/test_contract.py`, `tests/test_house.py`, `tests/test_legal_corpus.py`
- Enforced in: `brand/content.py`, `brand/house.py`, `scripts/house_rule.py`, `templates/youtube_thumb.py`

## D30 · A credit is not a licence

- **Tested by:** `tests/test_contract.py`
- Enforced in: `templates/youtube_thumb.py`

## D31 · The system emits copy, not just artwork

- **Tested by:** `tests/test_contract.py`

## D32 · A vertical clip under 60s is a Short, and Shorts have no thumbnail

- **Tested by:** `tests/test_contract.py`

## D33 · The opening frame is the cover

- **Not mechanically testable:** the opening frame is the cover — a composition choice

## D34 · Landscape is a different typographic problem, not a resize

- **Regression-guarded by the golden fingerprints** (landscape is a different typographic problem). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D35 · The progress bar is flush to the top edge

- **Regression-guarded by the golden fingerprints** (progress bar flush to the top edge). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D36 · A lower-third is a panel, not a gradient

- **Regression-guarded by the golden fingerprints** (a lower-third is a panel). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.
- Enforced in: `brand/motion.py`

## D37 · The bulletin carries the deck, and its length is derived

- **Tested by:** `tests/test_contract.py`
- Enforced in: `brand/motion.py`, `templates/bulletin.py`

## D38 · Landscape spends width on the type, not height

- **Regression-guarded by the golden fingerprints** (landscape spends width on the type). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.
- Enforced in: `brand/motion.py`, `brand/tokens.py`, `templates/youtube_thumb.py`

## D39 · A reel is the lead story, and it opens on the news

- **Tested by:** `tests/test_contract.py`
- Enforced in: `brand/motion.py`, `brand/tokens.py`, `templates/reel.py`

## D40 · The 4K bulletin is the same design at twice the size, not a bigger canvas

- **Regression-guarded by the golden fingerprints** (the 4K bulletin is the same design at twice the size). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D41 · The thumbnail hook is guarded like a headline, and set like one

- **Tested by:** `tests/test_contract.py`

## D42 · Kannada case markers agglutinate, and the ones we generate are checked

- **Tested by:** `tests/test_contract.py`

## D43 · The publishing plan is rendered, not retyped

- **Tested by:** `tests/test_contract.py`

## D44 · A bulletin target drops stories; it never rescales scenes

- **Tested by:** `tests/test_contract.py`

## D45 · A narrated reel is cut from the speech, never alongside it

- **Tested by:** `tests/test_contract.py`

## D46 · A glyph the face does not carry is a rendering fault, caught in the type engine

- **Tested by:** `tests/test_contract.py`

## D47 · The fact card is set at news size, and the end card holds a picture

- **Tested by:** `tests/test_contract.py`
- Enforced in: `brand/motion.py`

## D48 · Scenes wipe; the music ducks once

- **Tested by:** `tests/test_contract.py`

## D49 · The disclosure travels with the image, not with the story

- **Tested by:** `tests/test_calendar.py`, `tests/test_contract.py`
- Enforced in: `brand/motion.py`, `templates/greeting.py`

## D50 · Written Kannada and spoken Kannada are different languages

- **Tested by:** `tests/test_contract.py`
- Enforced in: `brand/codes.py`

## D51 · A reel card carries a short form, because the voice is faster than reading

- **Tested by:** `tests/test_contract.py`
- Enforced in: `brand/content.py`, `brand/motion.py`, `brand/tokens.py`

## D52 · The Chief Editor's green signal is established, not asserted

- **Tested by:** `tests/test_contract.py`

## D54 · A festival greeting is its own genre, not a news card with a festive photo

- **Tested by:** `tests/test_contract.py`, `tests/test_greeting.py`
- Enforced in: `brand/ornament.py`, `brand/review.py`, `scripts/render_wishes_poster.py`, `templates/__init__.py`, `templates/greeting.py`, `templates/registry.json`

## D55 · No source, no claim. No approval, no upload

- **Tested by:** `tests/test_contract.py`, `tests/test_intake.py`
- Enforced in: `brand/content.py`, `brand/copy.py`, `brand/review.py`, `scripts/draft_edition.py`

## D56 · Numeric policy lives in `tokens.Limits`

- **Tested by:** `tests/test_contract.py`, `tests/test_intake.py`
- Enforced in: `brand/codes.py`, `brand/house.py`, `brand/tokens.py`, `scripts/house_rule.py`

## D57 · Generated images, including stock, are `nature: 'ai'`

- **Tested by:** `tests/test_contract.py`
- Enforced in: `brand/codes.py`, `brand/content.py`

## D58 · A reel opens on the news

- **Tested by:** `tests/test_contract.py`
- Enforced in: `brand/voice.py`

## D59 · No human verification, no publication

- **Tested by:** `tests/test_calendar.py`, `tests/test_contract.py`, `tests/test_draft_edition.py`, `tests/test_intake.py`, `tests/test_verify.py`
- Enforced in: `brand/codes.py`, `brand/content.py`, `brand/review.py`, `scripts/draft_edition.py`, `scripts/fetch_daily_news.py`, `scripts/verify.py`

## D60 · Legibility is arithmetic, and arithmetic is testable

- **Tested by:** `tests/test_legibility.py`
- Enforced in: `brand/review.py`, `brand/tokens.py`

## D61 · The legal guard is a word list, so its coverage is measured

- **Tested by:** `tests/legal_corpus.json`, `tests/test_legal_corpus.py`
- Enforced in: `brand/content.py`

## D62 · The gate records what a machine established, and names who judged the rest

- **Tested by:** `tests/test_contract.py`

## D63 · The intake retrieves; it never supplies what the source lacks

- **Tested by:** `tests/test_intake.py`
- Enforced in: `scripts/draft_edition.py`

## D64 · Brand rules are gilded, not stamped

- **Regression-guarded by the golden fingerprints** (brand rules are gilded, not stamped). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D65 · The house grade gets a filmic highlight shoulder

- **Regression-guarded by the golden fingerprints** (the filmic highlight shoulder). A change here moves pixels and `tests/test_golden.py` fails. Note this catches a regression; it does not assert the rule.

## D66 · The editorial plate is a scene, not a backdrop

- **Not mechanically testable:** the plate is a designed scene; "reads as a graphic, not as a bad photograph" is a judgement, and the one it replaced passed every number while looking wrong
- Enforced in: `brand/surface.py`

## D67 · A decision with nothing behind it is a paragraph

- **Tested by:** `tests/test_contract.py`

## D68 · The reel gate cannot be the thing that limits crime

- **Tested by:** `tests/test_contract.py`, `tests/test_reach.py`
- Enforced in: `brand/reach.py`, `brand/review.py`, `brand/tokens.py`

## D69 · A bad Tuesday has a defined minimum, decided in advance

- **Tested by:** `tests/test_calendar.py`
- Enforced in: `scripts/draft_edition.py`

## D70 · The year is on a calendar, not in somebody's head

- **Tested by:** `tests/test_calendar.py`

## D71 · The scheduler is not ours to own; the heartbeat is

- **Tested by:** `tests/test_intake.py`

## D72 · Reach is local penetration, and it is measured that way

- **Tested by:** `tests/test_pick_formats.py`, `tests/test_reach.py`
- Enforced in: `brand/content.py`, `brand/review.py`, `brand/tokens.py`, `scripts/draft_edition.py`, `scripts/pick_formats.py`

## D73 · The newsroom answers in one shape, argues with itself, and remembers

- **Tested by:** `tests/test_house.py`

## D74 · A licence you cannot point at is one you cannot produce

- **Tested by:** `tests/test_contract.py`

## D75 · A URL two tips share is a listing page, not an article

- **Tested by:** `tests/test_draft_edition.py`, `tests/test_intake.py`
- Enforced in: `scripts/draft_edition.py`, `scripts/fetch_daily_news.py`

## D76 · "Ready by 8am" needs a number attached to how sure that is

- **Tested by:** `tests/test_draft_edition.py`, `tests/test_verify.py`

## D77 · Carousel is the day; a reel is earned, not defaulted

- **Tested by:** `tests/test_pick_formats.py`

## D78 · A tip is only worth having if its link opens and its flags mean something

- **Tested by:** `tests/test_intake.py`

